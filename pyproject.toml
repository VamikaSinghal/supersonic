[project]
name = "sonic"
version = "0.1.0"
description = "Supersonic: a local coding harness with a swappable planner"
requires-python = ">=3.12"

[project.scripts]
sonic = "sonic.__main__:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
